import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

PreferredSizeWidget App_Bar() {
  return AppBar(
    title: Text("Hello ahmed"),
    centerTitle: true,
    // leading: Drawer(child: Column(),),
  );
}
